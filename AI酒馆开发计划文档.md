# AI 酒馆(ai-roleplay)开发计划文档

> 版本:v1.1(状态同步于 2026-08 开发迭代)
> 依据:`public/sillytavern-vs-airoleplay.html`(系统化对比报告 v1.1,含后续计划)
> 适用范围:AI 酒馆 —— AI 角色扮演与多模型对话平台(Tauri 2 + Vue 3 + TypeScript)
> 用途:团队成员直接用于开发跟踪;任务编号(T-xx / E-xx)贯穿提交信息与 issue

---

## 〇、任务状态总览

| 任务 | 名称 | 状态 | 完成内容 |
|---|---|---|---|
| T-01 | Anthropic 直连 | ✅ 已完成 | `AnthropicClient` + 协议转换 + 24 项测试 |
| T-16 | max_tokens 空值化 | ✅ 已完成 | 设置面板可留空(不传 max_tokens,供应商默认);OpenAI/Tauri 链路本就可选;Anthropic 协议必填保留 4096 兜底 |
| T-17 | 前缀缓存命中率统计 | ✅ 已完成(全端+本地检测) | Web/Tauri usage 解析 + 本地代理检测:角色前缀稳定率(standing 段哈希对比)+ TTFT 统计;顶栏三态展示(供应商命中率/前缀稳定率);chat store 2 项测试 |
| T-02 | 工具调用 | ✅ 已完成 | 协议透传/解析 + `tool-registry` + ChatManager 循环 + chat store 装配 |
| T-03 | 长对话滚动加载 | ✅ 已完成 | 顶部自动加载 + 位置补偿 + 防重入,4 项组件测试 |
| T-04 | 本地推理流式 | ✅ 已完成 | 队列通道逐 token 输出 + 中止支持,4 项测试 |
| T-05 | 模型管理/降级/采样 | ✅ 已完成 | WASM 降级检测 + Top-P 参数 + UI 提示 |
| T-06 | 数据安全闭环 | ✅ 已完成 | 备份加密 + 审计日志(含设置页 UI)+ 威胁模型文档(docs/threat-model.md) |
| T-07 | ST 全格式互导 | ✅ 已完成 | 群聊/Quick Reply 互导 + 双端 UI 按钮;PNG/世界书原有 |
| T-08 | 故事引擎一键化(一~三) | ✅ 已完成 | 一键生成面板 + sourceContext 注入 + 组件测试;样例包待补 |
| T-09 | 扩展沙箱 v1(一期) | ✅ 已完成 | permissions 权限门禁(network/dom 默认拒绝)+ 拒绝守卫 + 3 项测试;Worker/iframe 真隔离为演进项 |
| T-10 | 性能基准页 | ✅ 已完成 | benchmark 核心(3 项微基准+预算断言+历史)+ LocalModelView 基准 tab;首个 token 云端对比待联调 |
| T-11 | 市场 v1(一期) | ✅ 已完成 | market-index 清单格式/校验 + sha256 哈希校验 + 11 项测试;下载/安装 UI 与真实仓库待接 |
| T-12 | 多 profile(一期) | ✅ 已完成 | storage-factory profile 隔离(Web 按档案分库)+ 设置页档案管理(创建/切换/回默认)+ 4 项测试;Tauri 文件系统档案隔离需 Rust 路径重构(ponytail) |
| T-13 | i18n 英文 | ⏳ 待开发 | — |
| T-14 | 工程基线与技术债 | 🔄 部分完成 | PlaceholderView 死代码删除、市场 Beta 徽标、sanitize 债登记、a11y 测试清理 |
| T-15 | 桌面端强化(一期) | ✅ 已完成 | 拖拽导入(JSON 角色卡/世界书+PNG 角色卡)+ 断网提示条 + 3 项测试;托盘/全局快捷键/updater 需 Rust 与发布源,待后续 |
| E-00 | 落地形态决策 | ✅ 已完成 | 决策:内置 TS 形态(docs/e00-decision.md);E-01~03 落点已冻结 |
| E-01 | L0 上下文结构层(核心) | ✅ 已完成 | memory-store.ts(版本化/写保护/前缀哈希/情绪状态机)+ 9 项测试;storage 持久化接线待续 |
| E-02 | L2 输出纪律层 | ✅ 已完成 | output-discipline.ts(旁白分类/保护哨兵/填充词压缩/Auto-Clarity)+ 12 项测试;ChatManager 接线待续 |
| E-03 | L1 内容压缩层 | ✅ 已完成 | compression.ts(提取式压缩/语义校验/CCR/触发决策/豁免)+ 17 项测试;chat-manager 挂载待续 |
| E-04 | 集成灰度 | ✅ 已完成 | optimization-pipeline(开关/阶段/统计/回退暂停)+ chat-manager 挂载(compressMessages)+ 设置页开关 UI + 9 项测试;真实灰度数据评估待二期 |

**测试基线:81 测试文件 / 2518 用例全绿 + Rust 35 测试,e2e 10/10;vite build 通过。**

**真实联调(2026-08,本地网关 192.168.9.40):** listModels/非流式(不传 max_tokens)/流式 SSE/工具调用 tool_calls 解析 4/4 通过,模型 oc/deepseek-v4-flash-free(reasoning,thinking 耗 token)。

---

## 一、项目功能升级概述与目标

### 1.1 背景与战略定位

本项目为独立角色扮演平台,对标 SillyTavern(生态之王但技术陈旧、安全裸奔)。战略定位:**现代工程 + 低门槛离线 + 创作深度**的错位竞争。当前处于 v0.1.0(约 6 万行代码,测试覆盖率 80% 门禁),功能骨架完整但存在三类短板:

1. **基本盘缺口**(P0):Anthropic 未直连、无工具调用、本地推理非流式、长对话无虚拟滚动、生态互导缺失;
2. **硬伤**(P1):扩展无沙箱、故事引擎未产品化、市场是 mock;
3. **质量成本线**(独立工作线):设定稳定性、情绪连贯、token 成本(嵌入优化三层方案)。

### 1.2 目标清单

| 编号 | 目标 | 优先级 | 目标版本 | 来源(报告章节) |
|---|---|---|---|---|
| G1 | 对话闭环体验达到 ST 同级(Anthropic/工具调用/流式/虚拟滚动) | 高 | v0.2 | §04 P0-1 |
| G2 | 本地推理产品化(流式、模型管理、降级) | 高 | v0.2 | §04 P0-2 |
| G3 | 数据安全闭环(备份加密、审计、威胁模型) | 高 | v0.2 | §04 P0-3 |
| G4 | ST 生态互通(全格式导入导出) | 高 | v0.2 | §04 P1-3 |
| G5 | 故事引擎产品化(一键流水线 + 模板库) | 中 | v0.3 | §04 P1-1 |
| G6 | 扩展沙箱 v1(隔离 + 权限声明) | 中 | v0.3 | §04 P1-2 |
| G7 | 嵌入优化三层(L0/L1/L2)上线 | 高 | 独立工作线 7 周 | §08 |
| G8 | 市场真实化(模板下载 v1) | 低 | v1.0 | §04 P2-1 |
| G9 | i18n + 多用户/多 profile | 低 | v1.0 | §04 P2-2 |
| G10 | 桌面端强化(updater/托盘/拖拽) | 中 | v0.3 | §05 优势强化 |

### 1.3 范围与边界(本计划不做)

- 不替换模型、不改用户交互主流程(嵌入优化为中间件形态);
- 不建自营云端服务器(市场 v1 用 GitHub 索引,无后端);
- 不与 ST 拼生态广度(只做互通,不做移植);
- 不做英文社区运营(v1.0 前)。

---

## 二、功能模块拆分

两条并行工作线:**主功能线**(模块 A–J)+ **嵌入优化线**(模块 K)。

### 模块总览

| 模块 | 名称 | 包含任务 | 优先级 | 目标版本 |
|---|---|---|---|---|
| A | 对话闭环补齐 | T-01, T-02, T-03 | 高 | v0.2 |
| B | 本地推理产品化 | T-04, T-05 | 高 | v0.2 |
| C | 数据安全闭环 | T-06 | 高 | v0.2 |
| D | ST 生态互通 | T-07 | 高 | v0.2 |
| E | 故事引擎产品化 | T-08 | 中 | v0.3 |
| F | 扩展沙箱 v1 | T-09 | 中 | v0.3 |
| G | 可观测与基准 | T-10 | 中 | v0.3 |
| H | 市场与社区 | T-11 | 低 | v1.0 |
| I | 平台扩展 | T-12, T-13, T-15 | 中/低 | v0.3–v1.0 |
| J | 工程基线与技术债 | T-14 | 中 | 持续 |
| K | 嵌入优化三层 | E-00 ~ E-04 | 高 | 独立 7 周 |

### 模块 A:对话闭环补齐(高)

| 任务 | 名称 |
|---|---|
| T-01 | Anthropic Messages API 直连 |
| T-02 | LLM 工具调用(tools 透传 + 内置工具) |
| T-03 | 长对话虚拟滚动 |

### 模块 B:本地推理产品化(高)

| 任务 | 名称 |
|---|---|
| T-04 | 本地推理 token 级流式 |
| T-05 | 模型下载/缓存管理 + WebGPU→WASM 降级 + 采样参数暴露 |

### 模块 C:数据安全闭环(高)

| 任务 | 名称 |
|---|---|
| T-06 | 备份加密、导入导出审计、威胁模型文档 |

### 模块 D:ST 生态互通(高)

| 任务 | 名称 |
|---|---|
| T-07 | ST 角色卡/世界书/群聊/Quick Reply 全格式导入导出 |

### 模块 E:故事引擎产品化(中)

| 任务 | 名称 |
|---|---|
| T-08 | 小说→可玩酒馆一键流水线 + 模板库 |

### 模块 F:扩展沙箱 v1(中)

| 任务 | 名称 |
|---|---|
| T-09 | iframe/Worker 隔离 + permissions 权限声明,替换裸 new Function |

### 模块 G:可观测与基准(中)

| 任务 | 名称 |
|---|---|
| T-10 | 性能基准页(推理速度/token 对比、对话滚动基准) |

### 模块 H:市场与社区(低)

| 任务 | 名称 |
|---|---|
| T-11 | 市场 v1:GitHub 仓库索引 + 模板/角色卡下载 |

### 模块 I:平台扩展(中/低)

| 任务 | 名称 |
|---|---|
| T-12 | 多 profile(本地多用户) |
| T-13 | i18n 英文 |
| T-15 | 桌面端强化(Tauri updater、托盘、拖拽导入) |

### 模块 J:工程基线与技术债(中,持续)

| 任务 | 名称 |
|---|---|
| T-14 | 死代码清理、测试加固、P0-P3 债编号机制、CI 门禁维护 |

### 模块 K:嵌入优化三层(高,独立工作线)

| 任务 | 名称 |
|---|---|
| E-00 | 落地形态决策(内置 TS vs Node 服务端) |
| E-01 | L0 上下文结构层(版本化记忆 + 前缀组装 + 情绪状态机 + 防覆盖) |
| E-02 | L2 输出纪律层(旁白分类 + 哨兵保护 + Auto-Clarity) |
| E-03 | L1 内容压缩层(提取式压缩 + CCR + 语义校验 + 豁免黑名单) |
| E-04 | 集成、灰度与验收(M0 基线 → M4 全量) |

---

## 三、任务优先级划分

### 高优先级(阻塞 v0.2 发布)

| 任务 | 名称 | 理由 |
|---|---|---|
| T-01 | Anthropic 直连 | 基本盘缺失,主流用户刚需 |
| T-02 | 工具调用 | 现代 LLM 核心能力,无则落后 |
| T-03 | 长对话虚拟滚动 | 10k 消息卡顿是留存杀手 |
| T-04 | 本地推理流式 | 离线卖点的体验地基 |
| T-05 | 模型管理/降级 | T-04 的配套,缺则本地不可用 |
| T-06 | 数据安全闭环 | 信任型用户的下单理由 |
| T-07 | ST 全格式互导 | 冷启动借 ST 存量资源的唯一路径 |
| E-00 | 落地形态决策 | 阻塞整个 K 模块 |
| E-01 | L0 上下文结构层 | 优化线核心,收益最大 |

### 中优先级(v0.3 交付)

| 任务 | 名称 | 理由 |
|---|---|---|
| T-08 | 故事引擎一键化 | 差异化护城河,需打磨 |
| T-09 | 扩展沙箱 v1 | 消除安全硬伤,为生态铺路 |
| T-10 | 性能基准页 | 支撑 G2 卖点的量化证据 |
| T-14 | 工程清理/测试加固 | 持续投入,防技术债堆积 |
| T-15 | 桌面端强化 | 提升原生体验完整度 |
| E-02 | L2 输出纪律层 | 独立于 L0,可并行 |
| E-03 | L1 内容压缩层 | 依赖 L0 前缀稳定,后置 |

### 低优先级(v1.0 及以后)

| 任务 | 名称 | 理由 |
|---|---|---|
| T-11 | 市场 v1 | 依赖 T-07/T-08 的资源量 |
| T-12 | 多 profile | 非核心场景 |
| T-13 | i18n 英文 | 中文市场优先,英文后置 |
| E-04 | 全量验收 | 依赖 E-01/02/03 全部完成 |

---

## 四、任务详情(描述 / 预期产出 / 依赖)

### T-01 Anthropic Messages API 直连
- **描述**:在 `src/api/` 新增 Anthropic 客户端,实现 Messages API(system/user/assistant 消息结构、`max_tokens`、`temperature`、SSE 流式),复用 `src/services/type-adapters.ts` 的消息格式转换层,接入 `src/api/index.ts` 的 `createApiClient` 工厂。
- **预期产出**:`AnthropicClient` 类 + 工厂分支 + 设置页新增 Anthropic provider 配置 + 单元测试(≥80% 覆盖)。
- **依赖**:无(仅依赖现有 `type-adapters.ts`)。
- **验收**:真实/ mock 端点跑通非流式 + 流式对话;错误分类 `ApiErrorKind` 正确映射。

### T-02 LLM 工具调用
- **描述**:OpenAI 兼容 `tools`/`tool_calls` 参数透传;实现工具注册表,将 `src/core/trigger-engine.ts` 暴露为内置工具(如 `get_var`、`search_lorebook`、`retrieve_document`);处理工具结果回填。
- **预期产出**:`tool-calling` 模块 + 工具注册 API + 对话循环工具执行测试。
- **依赖**:T-01(协议层统一后实现,避免重复改造)。
- **验收**:模型发起 tool_call → 本地执行 → 结果回填 → 模型续答,全链路测试通过。

### T-03 长对话虚拟滚动
- **描述**:`src/views/ChatView.vue` 消息列表改造为虚拟滚动(固定/动态行高两种方案评估),消息组件 memo 化,滚动位置保持。
- **预期产出**:虚拟滚动组件 + 10k 消息渲染性能测试(Playwright 断言滚动帧率/无卡顿)。
- **依赖**:无。
- **验收**:10k 消息滚动无卡顿;键盘导航与"跳至底部"功能不回归。

### T-04 本地推理 token 级流式
- **描述**:重写 `src/core/local-model-engine.ts` 与 `LocalApiClient.chatStream`,由"对外一次性返回"改为逐 token 回调(对齐 `OpenAIClient` 的 ReadableStream 接口)。
- **预期产出**:流式接口统一 + WebLLM generate 循环逐 token 推送 + 流式中止支持。
- **依赖**:无。
- **验收**:本地推理首 token <2s(4bit 模型);中止响应 <500ms;UI 逐字渲染。

### T-05 模型下载/缓存管理 + 降级 + 采样参数
- **描述**:模型清单(4bit 量化预设)、断点续传下载、磁盘缓存与容量管理;WebGPU 不可用时降级 WASM;`LocalModelView.vue` 暴露 temperature/top_p/max_tokens 等采样参数。
- **预期产出**:模型管理模块 + LocalModelView 增强 + 降级路径测试。
- **依赖**:T-04(流式接口先行)。
- **验收**:断点续传通过;无 WebGPU 环境自动降级并提示;采样参数生效。

### T-06 数据安全闭环
- **描述**:备份加密(AES-GCM,复用 `api-key-crypto.ts` 的 PBKDF2 派生)、导入导出审计日志、威胁模型文档(存放位置、密钥边界、扩展权限、SSRF 面)。
- **预期产出**:加密备份/恢复流程 + 审计记录模块 + `docs/threat-model.md`。
- **依赖**:无。
- **验收**:加密备份在无密钥时不可还原;审计日志可导出;威胁模型覆盖全部数据出入口。

### T-07 ST 全格式导入导出
- **描述**:完整导入/导出 ST 角色卡(V2,含 PNG tEXt 嵌入)、世界书 JSON、群聊 JSON、Quick Reply;反向导出;覆盖 `character-card.ts`、`lorebook.ts`、`group-chat.ts`、`type-adapters.ts`。
- **预期产出**:`st-importer`/`st-exporter` 模块 + 格式兼容测试(用 ST 官方样例卡做 golden test)。
- **依赖**:无。
- **验收**:ST 官方样例角色卡/世界书 100% 无损往返;字段映射表文档化。

### T-08 故事引擎一键化流水线
- **描述**:将 `story-analyzer`(3 档)→ `world-generator` → `npc-generator` → 角色卡/世界书导入串成向导式流水线;沉淀模板库(奇幻/科幻/现代/末日);目标"10 分钟出可玩场景"。
- **预期产出**:`StoryEngineView.vue` 向导化改造 + 模板包 + 端到端 demo 脚本。
- **依赖**:T-07(生成的卡/世界书需能导出为 ST 格式共享)。
- **验收**:录屏 demo 10 分钟出可玩场景;盲评样本集质量回归(设定一致率、情绪保留率)。

### T-09 扩展沙箱 v1
- **描述**:`src/core/extension-loader.ts` 的 `new Function` 执行改为 iframe(srcdoc + CSP)或 Web Worker 隔离;主线程暴露受限桥接 API;扩展 manifest 声明 `permissions` 白名单,loader 校验并可视化展示。
- **预期产出**:沙箱运行时 + 权限声明 schema + 扩展权限审计 UI。
- **依赖**:无(与 T-07 独立)。
- **验收**:恶意扩展(读取 localStorage/发请求)被沙箱拦截;既有扩展(白名单内)零改动运行。

### T-10 性能基准页
- **描述**:新增基准页面/模块:本地推理速度与 token 数对比 DeepSeek、长对话滚动基准、RAG 召回耗时,数据入 `inference-cache` 风格指标存储。
- **预期产出**:基准页 + 指标采集 + 性能预算断言(首屏 <1.5s、RAG <100ms)。
- **依赖**:T-04、T-03(有真流式与虚拟滚动后才可测)。
- **验收**:预算断言 CI 可跑;对比结果可视化。

### T-11 市场 v1
- **描述**:`src/core/community-market.ts` 的 mock 逻辑替换为 GitHub 仓库索引:JSON 清单 + 直链下载(角色卡/世界书/模板先行);下载校验(哈希)。
- **预期产出**:市场真实下载流 + 清单格式 + 下载进度/校验 UI。
- **依赖**:T-07、T-08(货源)。
- **验收**:从清单安装角色卡/模板到本地并可直接使用;哈希校验失败拒绝安装。

### T-12 多 profile
- **描述**:storage 层按 profile 分目录(Web:IndexedDB 库名后缀;Tauri:数据目录子目录),设置页新增 profile 管理。
- **预期产出**:profile 切换 + 数据隔离 + 迁移指引。
- **依赖**:无(依赖现有 `StorageAdapter` 接口)。
- **验收**:profile 间数据完全隔离;切换不丢数据。

### T-13 i18n 英文
- **描述**:抽取全部文案到 `i18n/`(en/zh),参照 ST locales 结构;集中管理,运行时切换。
- **预期产出**:i18n 框架 + en.json 全量翻译 + 语言切换设置。
- **依赖**:无。
- **验收**:切英文后无硬编码中文残留(扫描门禁)。

### T-14 工程基线与技术债
- **描述**:删除 `PlaceholderView.vue` 等死代码;community-market 标记 beta;补缺失测试至门禁;维护 P0-P3 缺陷编号机制与 CI 门禁(覆盖率 80%、typecheck、eslint)。
- **预期产出**:清理 PR + 测试补齐 + 债清单(编号登记)。
- **依赖**:持续进行。
- **验收**:CI 全绿;死代码扫描(未引用文件)清零。

### T-15 桌面端强化
- **描述**:Tauri updater 自动更新、系统托盘 + 全局快捷键、拖拽导入角色卡/PNG/世界书;断网检测提示。
- **预期产出**:`src-tauri/` 能力扩展 + 安装包发布脚本(Windows/macOS/Linux)。
- **依赖**:无。
- **验收**:三端安装包可装可更;托盘唤起 <1s;拖拽导入全格式可用。

### E-00 落地形态决策
- **描述**:决策嵌入优化三层在项目内的实现形态:①内置 TS(纯前端,压缩/情绪/前缀在浏览器内,契合离线卖点)或 ②新增 Node 服务端。输出决策文档(含性能、离线、安全三方面论证)。
- **预期产出**:决策文档 + 架构图 + 任务拆分(决定 E-01~03 的落点)。
- **依赖**:无(阻塞 K 模块全部任务)。
- **验收**:决策有量化依据(如 10k 消息压缩在浏览器内的耗时预算);团队评审通过。

### E-01 L0 上下文结构层
- **描述**:实现版本化记忆存储(稳定 ID + 修订号 + 快照 + diff,可复用 `character-version-control.ts` 范式)、角色卡前缀组装(字节稳定,每请求哈希对比告警)、会话级情绪状态机(独立于角色卡存储)、standing 事实防覆盖硬约束(仅人工可写)。
- **预期产出**:`MemoryStore`/`CharacterRegistry`/`EmotionTracker` 模块 + SQLite/IndexedDB 表迁移(scope/kind/revision/author 字段)。
- **依赖**:E-00。
- **验收**:前缀稳定率 100%;核心设定一致率 100%;standing 写保护注入测试通过。

### E-02 L2 输出纪律层
- **描述**:旁白分类(状态性可精简/情绪氛围豁免)、保护正则哨兵(对白/情绪词/设定实体硬豁免,参照 caveman-shrink 思路)、Auto-Clarity 软回退(安全/不可逆/情绪冲突/重复提问恢复完整表达)。
- **预期产出**:`OutputDiscipline` 模块 + 豁免测试集。
- **依赖**:E-00(可先于 E-01 实现,独立)。
- **验收**:情绪词保留率 ≥95%;对白/情绪旁白豁免测试通过;输出 token 节省 ≥15%。

### E-03 L1 内容压缩层
- **描述**:提取式压缩(TextCrusher 思路:句切分→新颖度/BM25 相关性/显著性打分→3-gram 去重→保留原句重组)、CCR 无损端到端(24h TTL)、语义校验(实体/情绪词保留率 ≥0.9,关键实体 1.0)、豁免黑名单(standing/情绪/世界观永不压缩)、三级回退(fail-open → 暂停 → /no-opt)。
- **预期产出**:`CompressionPipeline`/`ExtractiveCompressor`/`SemanticValidator`/`CcrStore` 模块,挂载于 `prompt-builder.ts` 之后。
- **依赖**:E-00、E-01(需前缀稳定后才有意义)。
- **验收**:长会话输入 token 节省 ≥20%;关键实体保留率 100%;校验失败静默回退生效。

### E-04 集成、灰度与验收
- **描述**:M0 基线采集 → L0 全量 → L2 5% → L1 1% 长会话灰度 → 逐步放开;每层复测指标表,不达标回滚;产出验收报告。
- **预期产出**:`baseline.json` + 灰度配置 + 评估脚本 + 验收报告。
- **依赖**:E-01、E-02、E-03。
- **验收**:全部指标达标(见 §1.2 G7 指标表);无 P0/P1 遗留问题。

---

## 五、里程碑划分与阶段目标

### 5.1 双线并行时间轴

```
主功能线  ── v0.2(约 8 周)────────── v0.3(约 8 周)─────── v1.0(约 8 周)
优化线    ── E-00 → E-01 ── E-02 ── E-03 ── E-04(独立 7 周,与 v0.2 中段起并行)
```

### 5.2 主功能线里程碑

| 里程碑 | 周期 | 交付任务 | 阶段目标 | 验收门 |
|---|---|---|---|---|
| **M0 基线清理** | 第 1–2 周 | T-14(清理启动)、E-00(决策) | 死代码清零、优化线形态定案 | CI 全绿;决策文档评审通过 |
| **M1 基本盘打平** | 第 3–6 周 | T-01, T-02, T-03, T-04, T-05 | 对话闭环体验达 ST 同级;本地推理真流式可用 | 各任务验收项全过;e2e 全绿 |
| **M2 v0.2 发布** | 第 7–8 周 | T-06, T-07 + 回归 | 安全闭环 + ST 生态互通;可对外内测 | 新功能测试 ≥80%;ST 样例无损往返 |
| **M3 深度差异化** | 第 9–12 周 | T-08, T-09, T-10, T-15 | 故事一键流水线 + 扩展沙箱 + 基准页 | 10 分钟出可玩场景 demo 视频;恶意扩展被拦截 |
| **M4 v0.3 发布** | 第 13–16 周 | T-08~T-10, T-15 收尾 + 债清扫 | 差异化能力成型;性能预算达标 | 性能预算断言 CI 可跑;盲评质量回归通过 |
| **M5 v1.0 发布** | 第 17–24 周 | T-11, T-12, T-13 | 市场/多 profile/i18n;文档站 + 安全白皮书 | 全新用户无文档 30 分钟上手 |

### 5.3 嵌入优化线里程碑(7 周,与主功能线并行)

| 里程碑 | 时间 | 交付物 | 验收标准 |
|---|---|---|---|
| **M0 基线** | 第 1 周末 | `baseline.json` + 评估脚本 | 全部指标可复跑,基线数据完整 |
| **M1 L0 上线** | 第 3 周末 | MemoryStore/CharacterRegistry/EmotionTracker | 前缀稳定率 100%;核心设定一致率 100%;standing 写保护生效 |
| **M2 L2 上线** | 第 4 周末 | OutputDiscipline | 情绪词保留率 ≥95%;豁免测试通过;token 节省 ≥15% |
| **M3 L1 灰度** | 第 6 周末 | 压缩管线 + CCR + 校验器 | 输入 token 节省 ≥20%;关键实体保留率 100%;回退生效 |
| **M4 全量验收** | 第 7 周末 | 全量灰度 + 验收报告 | 全部指标达标;无 P0/P1 遗留 |

### 5.4 优化线验收指标(汇总)

| 指标 | 目标值 | 测量方式 |
|---|---|---|
| 核心设定一致率(≤10 关键事实) | 100% | 设定抽取测试 |
| 全量设定一致率(20–50 事实) | ≥95% | 设定抽取测试,连续 3 次滚动通过 |
| 设定文件静默改写数 | 0 | 版本号 + 文件哈希审计 |
| 情绪词保留率 | ≥95% | 情绪词表覆盖率对比 |
| 情绪标签连贯率(相邻轮次) | ≥90% | 情绪标注器输出对比 |
| L0 前缀段字节稳定率 | 100% | 每请求对比前缀哈希 |
| 缓存命中率(观测项) | ≥60% | provider usage 统计 |
| 输出 token 节省(对话型) | 20–40% | 真实用量对比 |
| 首 token 延迟 p95 | ≤2.5s | 压测 |
| 压缩管线单次耗时 | ≤100ms(不含 LLM 摘要) | 埋点统计 |
| BM25 召回耗时 | 软 100ms / 硬 500ms | 埋点统计,超时降级 |

### 5.5 执行规则

1. **产能分配**:每迭代 60% 投高优先级稳定性、30% 差异化、10% 技术债;
2. **依赖红线**:T-02 等带依赖任务,前置未验收不得开工;E-01/03 阻塞于 E-00;
3. **灰度纪律**:优化线每层灰度后复测指标表,任一指标不达标即回滚该层;
4. **任务跟踪**:任务编号(T-xx/E-xx)写入 commit message 与 PR 标题,便于溯源;
5. **风险预案**(嵌入优化线):压缩击穿缓存 → 暂停 L1 二级回退;情绪漂移 → 扩大豁免词表;设定被改写 → 快照一键恢复 + 告警定位;延迟超标 → BM25 超时降级 + 压缩异步化。

---

*文档完。任务清单与里程碑随 `public/sillytavern-vs-airoleplay.html` 报告 v1.1 同步维护。*
